---
title: "MDX 테스트"
date: "2026-04-03"
category: "test"
tags:
  - test
summary: "MDX 기반 블로그 글의 기본 템플릿입니다."
draft: false
---

## 시작

이 글은 `content/posts` 폴더에 들어가는 MDX 예시입니다.

### 체크 포인트

- Frontmatter에 `title`, `date`, `category`, `summary`를 넣습니다.
- 파일명은 자동으로 슬러그가 됩니다.
- `draft: true`로 두면 목록에서 숨길 수 있습니다.

```ts
export const message = "Hello, MDX";
```